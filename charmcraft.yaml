# Learn more about charmcraft.yaml configuration at:
# https://juju.is/docs/sdk/charmcraft-config
type: charm

base: ubuntu@24.04
platforms:
  amd64:

requires:
  db-admin:
    interface: pgsql
    limit: 1
    scope: global

parts:
  charm:
    plugin: charm
    source: .
